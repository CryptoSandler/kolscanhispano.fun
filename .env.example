# Environment variables for kolscanhispano.fun.
#
# Real values live only in .env.local, which is gitignored and never echoed,
# logged, or pasted on a command line. This file documents the shape only.

# 32-byte, base64-encoded keys. They live in the host environment (Vercel)
# and never in the database (Neon): a database dump alone must not yield
# addresses. See SECURITY.md.
WALLET_ENC_KEY=
WALLET_HMAC_KEY=

# Base64url secret used to verify Helius webhook deliveries.
HELIUS_WEBHOOK_SECRET=

# Optional. Enables the Helius DAS fallback in prices.ts, which is the only
# way a mint DexScreener has never heard of ever acquires a symbol. Absent,
# that lookup is a documented no-op and those rows keep symbol = NULL --
# degraded, not broken. Costs 10 credits per mint against a 1,000,000/month
# free tier, which is why scripts/refresh-token-metadata.ts bounds both how
# many mints one run resolves and how often a symbol-less one is re-asked.
HELIUS_API_KEY=

# Optional. How many mints scripts/refresh-token-metadata.ts resolves in one
# run. Unset means its default of 30 -- one DexScreener request's worth. 0 is
# a real value and stops the step doing anything, with no edit and no deploy.
TOKEN_METADATA_LIMIT=

# Neon Postgres connection strings. Both must carry sslmode=verify-full, and
# this is enforced rather than documented: resolveConnectionString() refuses a
# string without it, because nothing in this repo passes an ssl option and the
# text of the secret is therefore the only thing that decides whether the
# connection is encrypted and verified.
#
# DATABASE_URL points at the "production" branch. TEST_DATABASE_URL points at a
# **local Postgres container**, not at Neon, since 2026-09-06 -- the suite ran
# 8.5 hours with 45 false failures against Neon on a flapping link, and 34
# seconds against the container. `~/.claude/GATES.md` carries the measurements.
#
#     docker run -d --name kolscan-pg \
#       -e POSTGRES_PASSWORD=kolscan -e POSTGRES_USER=kolscan -e POSTGRES_DB=kolscan_tests \
#       -p 55435:5432 postgres:18
#     npm run db:migrate:test
#
# `postgres:18` because all three Neon databases run 18.6: a suite on another
# major version stops being evidence about what gets deployed. `sslmode=disable`
# is accepted **only** against loopback, where there is no network segment to
# intercept -- see assertVerifyFull in src/lib/connection-identity.ts.
#
# The "tests" Neon branch still exists and still gets migrated; nothing routine
# reads it. The test suite refuses to run if TEST_DATABASE_URL and DATABASE_URL
# are equal.
#
# Migrating production is `npm run db:migrate -- --prod`. The flag is required:
# no flag used to mean production, which made the shortest command in the repo
# the only one that applied DDL without first asserting anything about its
# target.
DATABASE_URL=
TEST_DATABASE_URL=postgresql://kolscan:kolscan@localhost:55435/kolscan_tests?sslmode=disable

# The "preview" Neon branch, a child of "production" created with its data.
# Loaded into Vercel as DATABASE_URL for Preview deployments only, so a preview
# never writes to production -- and never to the "tests" branch, which the suite
# truncates.
#
# `npm run db:seed:preview` is the one piece of local tooling that reads this
# name, and it reads nothing else: it refuses to run if this is unset, if
# DATABASE_URL is unset, if the two name the same database, or if the target
# carries test_database_marker.
#
# The seed writes only `trade` rows and the dirty `position` marks that go with
# them; `position`, `pnl_daily` and `pnl_position_daily` are then derived from
# those trades by the real engine, in a child process running
# `scripts/recompute-dirty.ts` with DATABASE_URL set to *this* branch. The
# variable below is still the only target name it ever reads.
# Apply migrations here too when a branch adds one: `npm run db:migrate:preview`.
# A preview deployment reads this branch, so a schema left behind fails at runtime.
PREVIEW_DATABASE_URL=

# The admin gate (spec §9). Absent means every admin route refuses -- there is
# no "unset, so allow" path, deliberately: that ships an open admin route on
# exactly the deployment where somebody forgot to set it.
ADMIN_TOKEN=

# ── Robinhood Chain (EIP-155 4663) ──────────────────────────────────────────
#
# **The flag gates the offer, not just the parser.** `activeChains()` reads it in
# one place, so `/registro` and the profile cannot disagree about which chains
# exist. A wallet connected on a chain nothing indexes produces no trades and
# moves no rank — DESIGN.md's last Don't, "a control that does not work".
#
#   off / unset  the chain is invisible: no row in the wallet chooser, no
#                ingestion, nothing stored.
#   on           `/registro` offers Robinhood over EIP-6963 and the proof is
#                signed EIP-191 (`docs/wallet-proof.md` §2).
#
# **Turning it on does not start ingestion.** That is batch (3) and it is gated
# on a different thing: a real EVM wallet existing in the roster
# (`docs/round-robinhood.md` §3). Until then the column would be empty for every
# row, which is the state that reads as a broken tracker rather than a waiting
# one.
CHAIN_ROBINHOOD_INGESTION=off

# The RPC the ingestion will read, once there is something to read. Alchemy's
# `arrival` app today. **Not needed for registration**: a signature is produced
# by the reader's wallet and verified with `@noble/curves`, so `/registro` makes
# no RPC call at all — which is also why the money-path rules stay dormant here
# (`docs/wallet-warnings.md`).
#
# Budgeted: every call is logged to `evidencia/kolscan/cu-<mes>.jsonl` against
# the free tier's ceiling, and `docs/round-robinhood.md` §0 records that the
# percentage quoted for it could not be recomputed from that ledger.
# ROBINHOOD_RPC_URL=
