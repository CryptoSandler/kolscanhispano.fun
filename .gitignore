.env*.local
node_modules
.next
next-env.d.ts
*.tsbuildinfo

# Playwright
test-results
playwright-report
.playwright

# Raw Helius payloads: they carry real addresses and real signatures, and the
# signature is in the filename. Spec §8.3 keeps those out of the tree.
fixtures/helius

# Vercel project link: holds projectId and orgId, and this repo is public.
.vercel
